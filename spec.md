# Icomaker.bat (.py)の仕様

Icomaker.batはIcomaker.pyを実行します
Icomaker.pyはアイコンを作成します

## 動作
Icomaker.pyは次を行います
- スクリプトファイルのあるフォルダ/imgに存在する全てのpng画像を列挙します
- 画像が透明なマージンを持つ場合、そのマージンを削除します
- 削除結果の画像が正方形になるように余白を追加します。この時元画像は中央に配置されます
- 正方形の画像を256x256ピクセルにリサイズします
- スクリプトファイルのあるフォルダ/iconsにpng画像を保存します